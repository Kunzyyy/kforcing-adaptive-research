# 局部教师收益迁移：单独冻结的后续开发实验

2026-09-21。机制诊断结束后、任何本实验拟合前保存。

## 依据与阶段区分

已完成的local-mechanism诊断不训练模型。其固定局部教师排序相对原19维的完整续写收益差为+0.05147，95%描述区间[0.01461,0.08624]，达到预先设定的后续设计条件。因此在此另立一个有独立清单的开发实验，检查这条读取重算后信息的线索能否被重算前预测器利用；不修改前一诊断的计划、结果或“不训练模型”属性。

这是假设来源，不是新数据证据。两阶段都使用已多次复用的446状态、253源句，只能开发筛选，不能作独立确认。

## 唯一改变

- 输入仍是冻结的19维当前置信度、生成位置和EOS信息；候选输入不包含重算后候选、局部教师分数或任何完整未来评分。
- 训练标签改为上一诊断固定定义的局部共同长度AR教师NLL收益。该标签只在训练折使用，属于离线监督。
- 模型仍为训练折内标准化的ridge，alpha=1000。只拟合这一个候选local_target19。
- 原full_target19以相同折和参数拟合原8次完整续写GPT-2收益，作为原实现控制；所有折外预测及排序应复现之前combined19。
- 固定原3次5折源句划分，种子14112001/2/3；每折按预测选floor(N/2)，平手按state_id。
- 验证目标保持原8次完整续写GPT-2收益，不按新训练标签的好看结果替换任务。

## 固定推进标准

候选相对原19维、相对负尾部平均margin规则，两项97.5%双侧源句bootstrap区间的下界都必须大于0；4000次，seed22242002。两个比較的名义family95%，但反复开发仍使其不能视为确认性结论。

不比较两种不同标签单位下的原始MSE大小来判断优劣。仅辅助报告各模型对自己的训练目标的折外MSE、训练折常量基线，以及原任务排序收益。不搜索正则、符号、阈值、特征、样本子组或模型结构。

通过也仅意味着需要在全新源句上确认；不通过则停止本次局部标签迁移实现。本轮不再加模型容量或数据来追求通过。没有在线计时、人类质量或更好计算预算匹配结论。

## 核查

拟合前冻结协议、代码、19维特征、原开发数据、已完成局部教师评分及前序审计哈希。保存全部30次拟合和每状态折外预测；单独CPU程序以增广最小二乘重算拟合、源句隔离、排序及两项主要区间。旧14/19维、注意力和机制诊断证据保持不变。
