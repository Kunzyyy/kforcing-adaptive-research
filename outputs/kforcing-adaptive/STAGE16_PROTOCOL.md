# 第十六阶段：发布实现核查与扩大基线样本

2026-09-20，在新样本抽取和新结果产生前固定。本轮不调整自适应控制器。

## 待检验的两项问题

1. 本地便携推理是否与从发布权重独立实现的数学前向一致？旧缓存/完整前向检查共享部分实现；本轮另用权重字典、显式归一化、旋转矩阵、QK乘积/softmax/V以及MLP实现无缓存参考。
2. 第四阶段128前缀、两个种子的基线质量差距是否主要来自小样本？固定新1024个前缀、每个一个种子，只比较公开AR和固定K=4，扩大源句覆盖。

论文v2的表4写RMSNorm，而发布代码LayerNorm.forward调用F.layer_norm。这是需澄清的公开描述/实现差异，不是已知根因。本轮保持发布实现，不替换归一化。参数量按组件列出，区分Transformer块、输入嵌入、输出头、噪声编码器；不混淆参数统计口径和检查点身份。

## 先锁定、再执行

冻结本协议、执行和参考代码、发布模型源码、评分代码与输入哈希。安全读取weights_only=True，核对两权重SHA。独立前向预检通过后才启动新基线生成。

独立前向取阶段4固定K=4、seed617的前8条（按prompt_id排序）完整token记录，上下文长度6/10/14/30/70，k=1/2/3/4。合计160个上下文/宽度实例、400个候选位置。噪声CPU seed=16112003+length*101，tau=1，fp32、TF32关闭；不调用模型的forward、block、RoPE、SDPA或噪声编码函数来产生参考输出。参考只读取state_dict张量并使用基础PyTorch算子。记录逐实例最大logit误差、argmax差异、最小top1/top2间隔。预检容差为全logit最大绝对差<0.005，且全部400个候选argmax一致；不满足则保留结果并先定位，不运行新基线。较长上下文可能位于EOS之后，仅作数值路径诊断。

## 新基线样本与生成

抽样seed=16112001；从固定LM1B held-out parquet选择1024源句，沿用此前“至少12个BERT token、输入CLS+5词片”规则，排除阶段1–15已有前缀、已知源句行号及哈希。保持数据筛选一致以检验小样本解释；作者未公布确切前缀，本轮不声称完全对齐作者抽样。

batch=4，逐批sampler_seed=16120000+batch_index*101，两方法开始前重置随机种子；AR和PFLM采样实现不同，因此这不是逐token相同噪声的配对。使用公开CLI AR内部fp16及PFLM无外层autocast，tau=1、频率惩罚0。完整生成122新位置，质量在首个生成EOS截断。保存全部128token输出；仅比较质量，不发布性能结论。两方法各1024条，共2048条输出。

## 评分与分析

复用已验证GPT-2-Large fp32 completion-only、去特殊token、无BOS的shift NLL；少于2个GPT-2 token记录排除。保存每文本NLL总和与token数。主要指标为exp(全部NLL总和/全部评分token数)。新样本先评分，再评分旧阶段4的AR/K4控制512条，检查原评分在相对1e-5、绝对0.005总NLL容差内复现。

按1024个源句成组，两个方法保持配对，bootstrap4000次、seed=16112004；报告两个Gen-PPL的95%描述区间及K4/AR比值区间。论文AR104.8、K4 127.6仅作参照，不当作已知总体参数做显著性检验，不宣称复现/推翻论文。此诊断已受旧差异启发，新的源句仅用于检查本地差距能否重复，不作为自适应方法测试。

附加描述：首个生成词片以##开头的比例、输出长度/EOS、前8条固定样本、参数组件数量。不给这些探索关联作因果解释，不为接近论文数值而更换评分口径。

## 范围

本轮能够排查本地实现一致性和有限样本波动；不能确定未公开训练配置、论文权重身份，或替代Linux/H100/FlashAttention环境对照。基线差距即使缩小，也不改变阶段15自适应候选未通过的结论。材料可整理给作者审阅，但未经用户明确授权不发送。

来源：[论文v2表1/表4及附录D.2](https://arxiv.org/html/2606.10820v2)、[发布代码](https://github.com/alibaba-damo-academy/K-Forcing/tree/706caa332a69d509b7fc2fa53ac7819f381a8c78)。
